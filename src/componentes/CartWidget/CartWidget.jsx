
const CartWidget = () => {
  return (
    <div style={{background:'url(../cart-shopping-solid.svg) center center / contain no-repeat',height:'1.9rem',width:'2rem'}} onClick={(e)=>{console.dir(e)}}>
      <span>
        98
      </span>
    </div>
  )
}

export default CartWidget